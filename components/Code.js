import styled from 'styled-components'
import {darkish} from '../layout/colors'

export default styled.code`
  display: inline-block;
  padding: 0 2px;

  font-family: 'iA Writer Mono';
  font-size: 90%;
  color: ${darkish};
`
